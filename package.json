{
  "name": "agora-token-server",
  "version": "1.0.0",
  "description": "Agora Token Server for authentication",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "agora-access-token": "^2.0.4",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5",
    "pm2": "^5.3.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}